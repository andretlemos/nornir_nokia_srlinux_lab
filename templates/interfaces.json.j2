"interface": [
  {% for iface in host.data.interfaces if iface.name != "mgmt0" %}
  {
    "name": "{{ iface.name }}",
    "subinterface": [
      {
        "index": 0,
        "ipv4": {
          "address": [
            { "ip-prefix": "{{ iface.ip }}" }
          ]
        }
      }
    ]
  }{% if not loop.last %},{% endif %}
  {% endfor %}
]


